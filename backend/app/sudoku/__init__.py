"""Pure-Python Sudoku core: board vocabulary, sample puzzles, CP-SAT solver.

Imports stay explicit — `from app.sudoku.solver import solve` — so that each
module can also be run on its own: `python -m app.sudoku.solver`.
"""
